.venv/
__pycache__/
*.pyc
.pytest_cache/
chroma_db/
.env
.DS_Store
*.egg-info/
.streamlit/

# Generated pipeline artifacts (reproducible — never commit)
data/raw/
data/chunks/
data/embeddings/

# NOTE: data/vectordb/ IS committed (small, ~2.4MB) so Render has a working
# index out of the box and doesn't need the network-dependent ingest chain.
